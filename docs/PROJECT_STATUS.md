# プロジェクト状況

最終更新: 2026-07-18

## 現在の区切り

カード名単位の相場一覧とカード詳細画面は `main` へマージし、本番反映済みです。

機能ブランチ `motokiyo/price-history-chart` では、日次平均価格の推移グラフと履歴0件・1日分の表示を実装し、ローカル検証済みです。まだ `main` へのマージと本番反映は行っていません。

本番Supabaseには `canonical_market_details` マイグレーションを適用済みです。ローカルの対応ファイルは次です。

```text
supabase/migrations/20260717182317_canonical_market_details.sql
```

このマイグレーションで追加した参照用ビューとRPCを、一覧とカード詳細画面から利用しています。SupabaseのTypeScript型も本番DBから再生成済みです。

## 実装済み

- Next.js / Supabase / Vercelの基本構成
- 公開URLへの自動デプロイ
- カード名の日本語正規化検索
- ざっくり検索（既定）とパーペキ検索
- カード名入力とサジェストの統合
- 同名カードを`canonical_cards`でまとめ、収録版を`card_prints`で分離
- 収録版を指定しない価格登録
- 全角・半角の価格入力正規化と数字以外の除外
- 登録PINの14日間セッション
- 傷あり、特価、ストレージ、特価ストレージ等の価格属性
- 気になるカードの端末内保存
- 上昇を赤、下落を青、30日超の情報を灰色で表示する基礎
- 登録PINで保護した価格登録
- 公開DB権限の最小化
- カード名単位の現在平均を使った相場一覧
- `/cards/[id]`のカード詳細画面
- 販売最安値、買取最高値、最近の価格記録の表示
- 収録版価格フォールバック時の注意表示
- 日次平均価格の推移グラフと履歴の空状態（機能ブランチ）

## DB追加済み・画面未実装

- 注意属性を除外した最安値・最高値検索

現在の本番データは価格記録2件で、いずれも収録版指定ありです。そのため、指定なし価格による平均履歴はまだ空です。画面ではフォールバック価格として注意付きで表示します。

## 次に着手する順番

1. `motokiyo/price-history-chart` のPull Requestをレビューし、問題なければマージする
2. Vercel Previewで履歴0件の表示とスマホ表示を確認する
3. 属性除外フィルターを追加する
4. 店舗マスター、店舗候補、店舗追加承認を実装する
5. 地域絞り込みと直線距離絞り込みを追加する
6. 価格記録の修正申請・管理者承認を追加する

## 保留中

- デュエル・マスターズ公式カード全件の再取得と本番投入
  - 公開APIやCSVの提供が確認できていないため、再利用条件の確認後に実行する
- トレカの地図からの店舗名一回取得
  - 公開前に利用条件を確認し、住所や継続取得は対象外にする
- Google Mapsリンク、緯度経度、距離検索
  - 無料で維持できる範囲だけで実装する

## 既知の開発環境上の注意

一部の外付けSSD環境ではNext.jsの本番ビルド時に`EISDIR/readlink`が発生することがあります。型チェックはSSD上で行い、本番ビルドだけ内蔵NTFSドライブのCloneで確認するか、Vercelのビルド結果を確認してください。

